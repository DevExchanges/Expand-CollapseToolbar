<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="devexchanges.info.expandcollapseactionbar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ExpandableToolBarActivity"
            android:label="@string/expand" />
        <activity
            android:name=".activities.HiddenToolbarActivity"
            android:label="@string/hide" />
        <activity
            android:name=".activities.ParalaxToobarActivity"
            android:label="@string/paralax" />
    </application>

</manifest>
